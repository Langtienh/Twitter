export enum UserVerifyStatus {
  Unverify,
  Verifyed,
  Banbed
}

export enum UserRole {
  user,
  admin,
  dev
}

export enum TweetType {
  Tweet,
  ReTweet,
  Comment,
  QuoteTweet
}

export enum TweetAudiece {
  Everyone,
  TweetCircle
}

export enum MediaType {
  Video,
  Image
}

export enum TokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerifyToken
}
